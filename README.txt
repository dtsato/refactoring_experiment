On this branch, I extracted the seeSuggestedProducts method from CheckoutController into ProductsController, with step-by-step commits.

The second part was to introduce the Upsell collaborator, which required some manual steps along the way.
